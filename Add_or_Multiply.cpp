#include <bits/stdc++.h>
using namespace std;
#define ll long long int
int main()
{
	ll T, N;
	scanf("%lld", &T);
	while(T--) {
		scanf("%lld", &N);
		ll arr[N];
		for(int i = 0; i < N; i++)
		{
			//
		}
	}
	return 0;
}