#include <bits/stdc++.h>
using namespace std;
int rev_arr[maxn], ans[maxn], arr[maxn];
int main () {
	scanf ("%d", &t);
	while (t--) {
		scanf ("%d%d", &n, &q);
		for (int i = 0; i < n; ++i) {
			scanf ("%d", &num);
			for (int j = 0; j < num; ++j) {
				scanf ("%d", &data);
			}
		}
	}
	return 0;
}